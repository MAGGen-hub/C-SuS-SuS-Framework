Features
	OWS_Sys --operator word sequnce system
		init       --initialize function for modules
		iterator   --iterator function
		parcer     --final structure
		make_react --fast method to create custom reaction on operator/word
		fast_react --function to quicly run reaction on operator/word
	code
		event
		level
		priority
		lua
			base
			struct
			prior_affect
			

NEW Features
Main
	text
		dual_queue --(Operator/Word/Result systems)
	common
		event
		level
	code
		priority -- (text operator priorityzing sys)
		lua
			base
			cssc